/// Empty on purpose. The selfhost proxy signs with the pair it holds, so the
/// browser is handed nothing to sign with — and nothing to leak.
const String apiKey = '';

const String apiSecret = '';
